#!/usr/bin/env bash
# Disabled: the user maintains index.html and git (commit/push) manually.
# Previously this copied fairshare.html → index.html, then staged, committed, and pushed.
set -euo pipefail
exit 0
